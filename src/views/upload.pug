extends base.pug

block content
    if errorMessage
        span=errorMessage
    form(method="POST", enctype="multipart/form-data")
        label(for="video") Video File 
        input(type="file", accept="video/*", required, id="video", name="video")
        input(name="title", placeholder="Video Title", type="text", required, maxlength=80)
        input(name="description", placeholder="Description", type="text", required, minlength=10)
        input(name="hashtags", placeholder="Hashtags, separated by comma.", type="text")
        input(value="Upload Video", type="submit")